name: nginx server
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker build -t ghcr.io/grim_yawn/simple-server-aws:${{ github.sha }} .
